declare const __PATH_PREFIX__: string;

interface Window {
  asciinema: {
    player: {
      js: {
        /* eslint-disable */
        // No type definitions for asciinema-player 
        CreatePlayer: any;
        UnmountPlayer: any;
        /* eslint-enable */
      };
    };
  };
}
